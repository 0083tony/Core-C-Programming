#include<stdio.h>
int main(void) {
	int a, b, c;

	printf("구간 단속 소요 시간 (분)? ");
	scanf_s("%d", &a);
	printf("구간 단속 주행 거리 (km)? ");
	scanf_s("%d", &b);
	
	c = (60 / a) * b;

	printf("평균 속력은 %d km/h입니다. ", c);
	if (c > 99) {
		printf("구간 단속 과속입니다.");
	}
	else {
		printf("구간 단속 과속이 아닙니다.");
	}
}
#include<stdio.h>
int main(void) {
	int a, b = 0;

	for (;;) {
		printf("주차 시간(분)? ");
		scanf_s("%d", &a);

		if (a > 1440) {
			printf("주차 시간은 24시간을 넘을 수 없다.\n");
			
		}
		if (a < 31) {
			printf("주차 요금: 2000원\n");
			
		}
		else if (1440 > a > 30) {
			b = 2000 + (a - 30) / 10 * 1000;
		}
		if (a <1440 && a > 260) {
			printf("주차 요금: 25000원\n");
		}
		else if (1440 > a > 30) {
			printf("주차 요금: %d원\n", b + 1000);
			
		}
		if (a == 0) {
			break;
		}
	}
}
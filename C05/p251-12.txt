#include<stdio.h>
int main(void) {
	double a, b;
	int i;

	printf("원금? ");
	scanf_s("%lf", &a);
	printf("연이율(%%)? ");
	scanf_s("%lf", &b);

	double ir = a;

	for (i = 1; i < 11; ++i) {
		ir = ir * (1 + (b / 100));
		printf("%d년째 이자: %.2lf, 원리합계: %.2lf\n", i, ir, ir + a);
	}
}
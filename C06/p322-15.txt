#include<stdio.h>
#include<math.h>
int round_pos(int n) {
	int a = 0, c = 0;

	c = pow(10, n);
	a = (((1357 + (5 * c)) / (10 * c)) * (10 * c));
	return a;
}
int main(void) {
	
	for (int n = 0; n < 3; n++) {
	printf("%d번째 자리에서 반올림한 결과: %d\n", n, round_pos(n));
	}
}
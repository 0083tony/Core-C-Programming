#include<stdio.h>
float get_area_of_squarer(float a) {

	a = a * a;
	return a;
}
int main(void) {
	float a;
	
	printf("한 변의 길이? ");
	scanf_s("%f", &a);
	

	printf("정사각형의 면적: %f", get_area_of_squarer(a));
}
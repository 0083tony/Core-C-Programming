#include<stdio.h>
#include<math.h>
int main(void) {
	int a, b;
	
	printf("금액? ");
	scanf_s("%d", &a);
	printf("절사단위 (10의 지수승)?");
	scanf_s("%d", &b);

	b = pow(10, b);
	a = a - (a % b);

	printf("금액(%d 미만 절사): %d", b, a);

}
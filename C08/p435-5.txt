#include<stdio.h>
int main(void) {
	int arr[10];
	int a, b, * p;

	printf("첫번째 항? ");
	scanf_s("%d", &a);
	printf("공창? ");
	scanf_s("%d", &b);

	arr[0] = a;
	p = &arr[0];

	printf("등차수열: ");
	for (int i = 0; i < 10; i++) {
		printf("%d ", *p);
		*p = *p + b;
	}
}
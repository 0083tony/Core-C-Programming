#include<stdio.h>
void fill_array(int* parr, int* a) {
	*parr = *a;
}
int main(void) {
	int arr[20];
	int a;

	printf("배열의 원소에 저장할 값? ");
	scanf_s("%d", &a);

	printf("배열: ");
	for (int i = 0; i < 20; i++) {
		fill_array(arr, &a);
		printf("%d ", *arr);
	}
}
#include<stdio.h>
#include<stdlib.h>
int make_graph(int a) {
	a = a / 100;

	for (int i = 0; i <= a; ++i) {
		printf("*");
	}
	printf("\n");
	return 0;
}
int main(void) {
	int i;
	
	for (i = 0; i < 3; i++) {
		int a = rand() % 10000;
		printf("%d: ", a);
		make_graph(a);
	}
}